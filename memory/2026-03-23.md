
## Memory Additions

- Added Excel export workflow to SDR automation. Introduced `scripts/generate_leads_excel.py` that reads `.sent` leads, formats them with links and summary, and writes `output/leads_<timestamp>.xlsx`. Documentation now instructs the bot to attach the Excel file before sending the final summary.
- Auth issues for SDR agent resolved by copying `auth-profiles.json` from the main agent and restarting the gateway so Anthropic tokens are available to the SDR bot.
- Logging plan: designed per-call usage logging (model/provider aware) writing JSON lines to `/home/ubuntu/.openclaw/usage/usage_log.jsonl`, but needs gateway file path to patch.
- Observed that Shubham’s entire 2026-03-23 run (onboarding + pillar + content pieces) consumed ~12.2k Claude Sonnet tokens (~$0.07 @ Sonnet-6) and Vaibhav’s CE session consumed ~30.6k tokens (~$0.18), with main-agent session cost negligible.
- Noted guidance: spawn cheaper sub-agents (haiku/mini) for heavy debugging or reverse engineering (e.g., Wellfound) to keep main session tokens low.
