# 2026-03-19 — Session Notes

## SDR Automation Bot — Built Today

### Architecture
- **New Telegram bot** created and wired as second bot alongside Shrebot
- Bot token: `8755718188:AAEBizKaWY9rekbF5K_n__cTwcAkTIeA-4E`
- Agent ID: `sdr`, workspace: `/home/ubuntu/.openclaw/workspace-sdr/`
- Binding: `telegram accountId=sdr → sdr agent`
- Shrebot (main agent, default account) completely untouched

### Config changes
- `openclaw doctor --fix` migrated Shrebot's top-level telegram config → `accounts.default`
- Both bots explicitly bound in `bindings[]`

### SDR Bot — What It Does
1. **Step 1** (`step1_fetch_jobs.js`): LinkedIn Voyager API → companies hiring SDRs
   - Needs `li_at` + `JSESSIONID` cookies in `session/linkedin.json`
   - Current session: Sales Navigator account cookies
2. **Step 2** (`step2_salesnav.js`): Sales Navigator people search → decision makers
   - Filters by seniority: OWNER, PARTNER, CXO, VP (Sales Nav levels)
   - Hard client-side filter: excludes interns, analysts, engineers, HR, recruiters etc.
   - New page per company (memory isolation), batch size 5, browser restart between batches
   - Blocks images/media/fonts but NOT stylesheets (Sales Nav needs CSS to render)
   - Checkpoint saves after every company — crash-safe
   - Streams `COMPANY_BLOCK_START/END` blocks to stdout for agent to relay to Telegram

### Scan formula
- 15 leads → scan 60 companies (Step 1), target 15 companies with DMs (Step 2 stops early)
- N leads → N×4 companies scanned, stop at N companies with DMs

### Session credentials (current)
- `li_at`: Sales Navigator account (long token starting with AQEFAREBAAAAABjNffQ...)
- `JSESSIONID`: `"ajax:2689896008412439311"` (domain: .www.linkedin.com)
- Previous li_at (personal account): AQEDAWXsJugBWPy7... (hit 403 on Voyager)

### Output format — company-grouped
```
🏢 Company · Location
💼 Hiring: [job title]
👤 Name — Title
   linkedin.com/sales/lead/...
```

### SKILL.md scan flow
- User sends `scan` → bot replies with start message + estimated time
- Step 1 runs, then Step 2 runs company by company
- Agent reads COMPANY_BLOCK_START/END from stdout, sends each block to Telegram immediately
- Final `SCAN_COMPLETE` marker triggers summary message

### Leads to clean up
- "Hiring Partner" (recruiters at Insight Global) and "Product Owner" (Aviso) slipped through
- Need to add to EXCLUDE_KEYWORDS: `hiring partner`, `product owner`

### Multi-user notes
- Session management: each Telegram user gets their own OpenClaw session (per-channel-peer)
- Vaibhav (Misal Vaibhav, id: 8176450202) — session cleared on request
- All users share same SDR workspace currently (not per-user isolated yet)
- Bot clears sessions via: delete .jsonl file + remove entry from sessions.json

### Files
- `workspace-sdr/skills/sdr-automation/SKILL.md` — full bot behaviour spec
- `workspace-sdr/skills/sdr-automation/scripts/step1_fetch_jobs.js`
- `workspace-sdr/skills/sdr-automation/scripts/step2_salesnav.js`
- `workspace-sdr/skills/sdr-automation/session/linkedin.json`
- `workspace-sdr/skills/sdr-automation/output/leads_latest.json`

## Update — 20:01 UTC

### Critical lesson: Can't send outbound as SDR bot from main agent
- The `message` tool in main agent always sends via Shrebot (default Telegram account)
- To send AS the SDR bot, it must come from the SDR agent's own reply mechanism
- Cross-agent `sessions_send` is blocked (visibility=restricted)
- Fix: SKILL.md updated with ⚠️ CRITICAL RULE — send start message FIRST before any exec

### Vaibhav's scan (19:52 UTC)
- Bot silently ran `step2_find_dms.js` (old script) without messaging Vaibhav first
- Old script killed (pid 202948/202949)
- Attempted to send apology message but it went via Shrebot by mistake — wrong bot
- Vaibhav needs to resend scan command to SDR bot for clean restart

### SKILL.md fixes applied
- "ALWAYS send start message FIRST before any exec" — front and centre
- step2_salesnav.js locked in as the only valid Step 2 script (never step2_find_dms.js)

### Step 1 issue
- `step1_fetch_jobs.js` crashed with SIGTERM (browser closed) on India location test
- Likely needs a longer timeout or browser fix — investigate next session
