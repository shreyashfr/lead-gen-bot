- reorganized workspace: canonical content-engine skills symlinked from root, created structures/ for grouped views (content-engine, sdr-automation, user-workspaces) with README, rebuilt openclaw viewer to target /home/ubuntu/.openclaw/workspace and added service env var.

- moved every content-engine skill (including ai-humanizer) into `skills/content-engine/…` and replaced the old root folders with symlinks + introduced `skills/sdr-automation/…` as the canonical home for the scouts so the top-level names stay valid while the code lives inside tidy groupings. 
- created `user-workspaces/` to house all Telegram workspaces, kept `users/<id>` as symlinks, and relocated the template file next to them so the files are grouped without breaking services.
- centralized Stripe webhook logs inside `logs/stripe-webhook/` and symlinked the original log paths so the uptime scripts keep writing to the same filenames.
- removed the temporary `structures/` overlay so there’s only one source-of-truth under `skills/`; the content-engine and SDR automation directories are now the canonical locations and no extra folders exist outside `skills/` for them.
- grouped every strategic brief Markdown into `content-engine/briefs/` (moved the handful of root copies plus the older files from `content-engine/` root there) and updated the coordinator agent + pending-ideas notes to save/read from the new path so the workspace root stays clean and all briefs live under the content-engine umbrella.
- deleted legacy `twitter-scout/`, `reddit-scout/`, and `reddit-scout-manual/` root-run archives because those scouts now run through the content-engine skills; cleaned up all the stray debug PNGs so the workspace root no longer publishes old artifacts.
